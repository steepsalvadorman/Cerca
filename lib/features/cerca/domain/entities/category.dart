/// A selectable service or project category chip (e.g. "Gasfitería").
class Category {
  const Category({required this.id, required this.label, required this.mono});

  final String id;
  final String label;
  final String mono;
}
